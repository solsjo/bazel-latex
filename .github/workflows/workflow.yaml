name: Build and Deploy
on: [push]
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
      - uses: mishas/setup-bazelisk-action@v1
      - name: Mount bazel cache  # Optional
        uses: actions/cache@v1
        with:
          path: "~/.cache/bazel"
          key: bazel
      - name: build
        shell: bash
        run: >
            bazelisk build //example:example_svg;
            #ls -a && cd bazel-out && ls -a && find -iname "*libgs*";
            #cd /home/runner/.cache/bazel/_bazel_runner/035df82e99f563e965083a2d561c9b1c/sandbox/linux-sandbox/2/execroot/bazel_latex && ls -a && find -iname "*.so" && find -iname "*lib*";
            #bazelisk build //...;
